# AWS Target Architecture

The AWS target mapping is defined in [aws-architecture.mmd](aws-architecture.mmd).

It maps the local services to ECS Fargate, Cloud Map, RDS, ElastiCache, ALB, WAF, CloudFront, and Route 53. This is a target design, not a deployed environment.

# Security Architecture

The security control plane is defined in [security-architecture.mmd](security-architecture.mmd).

It shows the intended relationships between identity, secrets, encryption, logging, audit, detection, and private data services.
